{
  "name": "goldenartframes",
  "version": "1.0.0",
  "description": "MVP: Print editor flow (portrait) - upload -> crop/zoom -> price -> add to cart.",
  "main": "server/index.js",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client --legacy-peer-deps && npm run build --prefix client",
    "start": "node server/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
